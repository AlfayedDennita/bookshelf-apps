const CUSTOM_EVENTS = {
  MODAL_CLOSED: 'modal-closed',
};

Object.freeze(CUSTOM_EVENTS);

const STORAGE_KEYS = {
  THEME: 'theme',
  BOOKS: 'books',
}

Object.freeze(STORAGE_KEYS);

export { CUSTOM_EVENTS, STORAGE_KEYS };
